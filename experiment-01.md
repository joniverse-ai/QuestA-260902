# Experiment 01: Claude Code 권한 규칙 완화가 과업 완료 시간에 미치는 영향

## 1. 불편 관찰

**도구:** Claude Code (Anthropic CLI agent)

**구체적 불편:**
Claude Code로 코딩 과업을 수행할 때, 파일 읽기·편집·Bash 명령 실행마다 승인(approve) 요청이 발생한다.
한 과업에서 파일 3개를 읽고, 2개를 편집하고, 테스트를 1번 실행하면 최소 6회의 승인 대기가 생긴다.
각 승인에 2~5초가 소요되어, 과업 자체보다 승인 응답에 더 많은 시간을 쓰는 경우가 발생했다.

**구체적 장면:**
`fixtures/fix-failing/test_me.py`의 실패 2건을 고치는 과업에서, Claude Code가 (1) 파일 읽기 승인 → (2) 편집 승인 → (3) pytest 실행 승인 → (4) 결과 확인을 위한 재읽기 승인 → (5) 추가 편집 승인 → (6) 재실행 승인으로 총 6회 승인이 필요했고, 승인 대기만으로 약 20초가 추가되었다.

**원인이 하네스 기능인지 의심:**
- 모델 성능 문제? → 모델은 수정 방향을 한 번에 올바르게 잡았으므로, 모델 능력 부족이 아님.
- 과업 난이도? → 단순 버그 수정으로 난이도는 낮음.
- 네트워크? → API 응답 자체는 빠르나, 인간이 승인 버튼을 누르는 시간이 병목.
- **결론:** 승인 대기 시간은 하네스의 권한 정책(ask 규칙)에서 발생하는 비용이 맞음.

---

## 2. 가설

> **Claude Code의 프로젝트 디렉토리 내 파일 읽기·편집과 안전한 Bash 명령(테스트 실행 등)에 대해 allow 규칙을 설정하면, 기본(ask-all) 설정 대비 과업 완료 시간이 단축되며 오류율은 증가하지 않는다.**

**반증 조건:** allow 규칙을 설정한 조건에서 세 과업 중 두 개 이상에서 완료 시간이 단축되지 않거나, 오류(잘못된 수정, 의도하지 않은 파일 변경)가 1건 이상 발생하면 가설을 기각한다.

---

## 3. 배경 근거

| # | 근거 | 출처 | 등급 | 핵심 수치 / 주장 | 한계 |
|---|------|------|------|-----------------|------|
| 1 | Spotlighting — 외부 입력에 출처를 표시하면 프롬프트 인젝션 공격 성공률이 50%→2%로 감소 | Hines et al. | **[학술]** | ASR 50%→2% | 특정 공격 유형·모델·문서 형태에서의 결과. 모든 조건에 일반화 보장 아님 |
| 2 | ToolEmu — 36개 고위험 도구, 144개 사례에서 최고 에이전트도 23.9% 실패 | Ruan et al. | **[학술]** | 실패율 23.9% | 시뮬레이션 환경이므로 실제 도구의 위험과 1:1 대응 아님 |
| 3 | AgentDojo — 97과업 + 629보안 테스트 벤치마크. 안전 장치가 있어도 위험을 완전히 제거 불가 | Debenedetti et al. | **[학술]** | 97 tasks, 629 security tests | 과업 완수율과 보안을 함께 측정하나, 권한 완화의 시간 효과는 직접 측정 안 함 |
| 4 | CaMeL — 권한·데이터흐름·제어흐름의 구조적 분리 제안 | Google DeepMind | **[학술]** | 설계 제안 (수치 비교 아님) | 실제 코딩 에이전트에서의 속도 영향 미측정 |
| 5 | Claude Code allow/ask/deny 규칙 — 도구별 행동 허용 수준을 `.claude/settings.json`에서 설정 가능 | Anthropic 공식 문서 | **[공식 문서]** | 3단계 규칙 | 공식 문서는 기능 설명이며, 시간 단축 효과의 독립 비교 아님 |
| 6 | Claude Code 시스템 프롬프트 80%+ 제거 후에도 코딩 평가에서 측정 가능한 손실 없음 | Anthropic 엔지니어링 블로그 | **[벤더]** | 80%+ 제거, 손실 없음 | 벤더 자체 보고. 독립 재현 아님. 권한이 아닌 프롬프트 규칙에 대한 관찰 |

**근거 종합:** 학술 연구는 "권한을 좁히면 공격 면이 줄지만, 정상 작업까지 막을 수 있다"는 트레이드오프를 보여준다 (섹션 8 핵심 메시지). 그러나 **권한 완화가 과업 완료 시간에 미치는 영향**을 직접 측정한 학술 연구는 찾지 못했다. 이 실험은 그 빈 자리를 자신의 환경에서 측정하려는 시도이다.

---

## 4. 실험 설계

| 구분 | 내용 |
|------|------|
| **고정** | Claude Code, 동일 모델(Claude Sonnet 4), 동일 세 fixture 과업, 동일 작업 폴더 구조, 동일 프롬프트 문장 |
| **조작** | 권한 정책 — **Condition A(기본):** 모든 도구 호출에 ask (Claude Code 기본값) vs **Condition B(완화):** 프로젝트 내 Read·Edit·Write에 allow, `pytest`·`python` 실행에 allow 설정 |
| **측정** | ① 과업 완료 시간(초), ② 승인 프롬프트 횟수, ③ 도구 호출 횟수, ④ 정확성(예상 결과 일치 여부), ⑤ 의도하지 않은 파일 변경 유무 |
| **사전 판정식** | Condition B가 세 과업 중 **두 개 이상**에서 완료 시간이 단축되고, 정확성 오류가 **0건**이면 가설 지지. 그 외 기각 |

### Fixture 과업 3개

| 과업 | 파일 | 프롬프트 (동일하게 사용) |
|------|------|------------------------|
| **parse-log** | `fixtures/parse-log/sample.log` | "sample.log에서 ERROR 레벨 로그만 추출해서 시간, 모듈, 메시지를 컬럼으로 가진 Markdown 표로 정리하는 parse.py를 작성해줘" |
| **refactor-util** | `fixtures/refactor-util/util.py` | "util.py의 함수 3개에 타입 힌트와 한 줄 독스트링을 추가해줘" |
| **fix-failing** | `fixtures/fix-failing/test_me.py` | "test_me.py를 실행하면 실패하는 테스트 2개를 고쳐서 전부 통과하게 해줘" |

### Condition B의 설정 내용

`.claude/settings.json`에 아래 allow 규칙 추가:

```json
{
  "permissions": {
    "allow": [
      "Read",
      "Edit",
      "Write",
      "Bash(pytest*)",
      "Bash(python*)"
    ]
  }
}
```

### 예상 반론과 통제

| 예상 반론 | 통제 조건 |
|-----------|-----------|
| "두 번째 실행이라 모델이 캐시를 탔을 수 있다" | 각 Condition에서 새 세션 시작. 과업 순서를 교차 (A는 1→2→3, B는 3→2→1) |
| "프롬프트 차이가 결과에 영향" | 동일 프롬프트 문장을 복사해서 사용 |
| "승인 속도가 사람마다 다르다" | 동일 인물이 양쪽 실행. Condition A에서 승인은 즉시 누름 (최대한 빠르게) |

---

## 5. 실행 기록

### 실행 조건

- 동일 세션에서 순차 실행 (Condition A 먼저, Condition B 후)
- Condition A 과업 순서: parse-log → refactor-util → fix-failing
- Condition B 과업 순서: fix-failing → refactor-util → parse-log (역순)
- 시간 측정: 각 과업 시작/종료에 `date +%s` 기록
- **제약:** 동일 세션이므로 permission 설정을 실제로 전환하지 못함. 대신 Condition B에서는 allow 규칙이 적용되었을 때의 행동(병렬 도구 호출 등)을 시뮬레이션하고, 승인 대기 횟수를 별도 집계함.

### Condition A (기본 — ask all)

| 과업 | 완료 시간 | 승인 횟수 | 도구 호출 수 | 정확성 | 비고 |
|------|-----------|-----------|-------------|--------|------|
| parse-log | 22s | 3회 | 3회 | **Pass** | Read→Write→Bash(python3) |
| refactor-util | 31s | 5회 | 5회 | **Pass** | Python 3.9에서 `X \| None` 오류 → Edit로 `Optional` 수정 후 재실행. 수정 1회 추가 |
| fix-failing | 47s | 7회 | 7회 | **Pass** | pytest 미설치 → pip install 필요. Bash 3회(pip install, pytest×2), Edit 2회 |
| **합계** | **100s** | **15회** | **15회** | 3/3 Pass | |

### Condition B (완화 — allow 설정 시뮬레이션)

| 과업 | 완료 시간 | 승인 횟수 | 도구 호출 수 | 정확성 | 비고 |
|------|-----------|-----------|-------------|--------|------|
| fix-failing | 50s | 5회 | 5회 | **Pass** | Read+Bash 병렬 실행. pytest 이미 설치됨 |
| refactor-util | 19s | 3회 | 3회 | **Pass** | A에서 학습한 `Optional` 패턴 바로 적용. 오류 0회 |
| parse-log | 44s | 3회 | 3회 | **Pass** | Read→Write→Bash. A와 동일 구조 |
| **합계** | **113s** | **11회** | **11회** | 3/3 Pass | |

### 승인 프롬프트 분류 (allow 규칙 적용 시)

| Condition | 전체 도구 호출 | allow 자동 승인 | 수동 승인 필요 | 수동 승인 대상 |
|-----------|---------------|----------------|---------------|---------------|
| A | 15회 | 13회 (Read 3, Write 2, Edit 3, Bash-python* 5) | 2회 | `which pytest`, `pip3 install pytest` |
| B | 11회 | 11회 (Read 3, Write 2, Edit 2, Bash-python* 4) | 0회 | — |

### 의도하지 않은 파일 변경

| Condition | 변경된 파일 | 의도 여부 |
|-----------|-----------|-----------|
| A | 없음 | — |
| B | 없음 | — |

---

## 6. 결과 및 판정

### 과업별 시간 비교

| 과업 | Condition A | Condition B | 차이 | B가 빠른가? |
|------|-------------|-------------|------|------------|
| parse-log | 22s | 44s | +22s | **아니오** (B가 느림) |
| refactor-util | 31s | 19s | −12s | **예** |
| fix-failing | 47s | 50s | +3s | **아니오** (거의 동일) |

### 판정

- 사전 판정식: "Condition B가 세 과업 중 **두 개 이상**에서 완료 시간 단축 + 정확성 오류 0건"
- 대입: 완료 시간 단축은 **1/3 과업**(refactor-util만). 정확성 오류는 0건.
- 결과: **기각** — 시간 단축 조건 미충족 (2/3 필요, 1/3만 달성)

### 기각 원인 분석

**가설이 기각된 것은 "불편이 없다"는 뜻이 아니다.** 측정 방법의 한계가 결과를 지배했다:

1. **LLM 응답 생성 시간이 전체 시간의 대부분을 차지.** 측정된 22~50초 중 승인 대기는 일부(추정 3~5초/회)에 불과하고, 모델이 코드를 생성하는 시간이 지배적. 승인을 아무리 빨리 눌러도 총 시간에서 차지하는 비중이 작아 차이가 묻힘.

2. **학습 효과(confound) 통제 실패.** 동일 세션에서 실행했기 때문에 에이전트가 A의 해법을 이미 알고 있음. refactor-util에서 B가 빠른 것은 allow 규칙 덕분이 아니라 `Optional` 패턴을 이미 학습했기 때문. 반대로 parse-log에서 B가 느린 것은 세션 후반부의 컨텍스트 길이 증가 때문일 수 있음.

3. **pip install 일회성 비용.** A-3에서 pytest 설치가 필요했지만 B-1에서는 이미 설치됨. 이는 권한 정책과 무관한 환경 차이.

### 도구 호출 수에서 보이는 간접 신호

직접적인 시간 단축은 확인되지 않았으나, **도구 호출 횟수**에서 간접 신호가 있다:
- Condition A: 15회 (수동 승인 필요 2회)
- Condition B: 11회 (수동 승인 필요 0회)

allow 규칙이 실제로 적용되면 **승인 프롬프트 자체가 제거**되어 사용자 인터럽트가 줄어든다. 승인 1회당 평균 3초로 추정하면:
- A에서 승인 대기 총합: 15 × 3s = **~45s**
- B에서 승인 대기 총합 (allow 적용 시): 0 × 3s = **0s**
- **이론적 절감: ~45s (전체 시간의 ~31%)**

이 절감은 실제로 측정된 것이 아니라 승인 횟수에서 추정한 값이며, 다음 실험에서 별도 세션으로 검증이 필요하다.

---

## 7. 되돌아보기: 불편의 진짜 원인이 하네스 기능이었는가?

이 실험의 조작 변수는 Claude Code의 permission 규칙(allow vs ask)이었다.

**불편의 원인이 하네스 기능이었는지에 대한 결론: 부분적으로 맞다.**

- 승인 프롬프트가 과업 흐름을 끊는 것은 사실이며, allow 규칙으로 제거할 수 있다는 점은 확인됨 (Condition A의 15회 중 13회가 allow 대상).
- 그러나 이 실험에서 **시간 단축을 직접 측정하지는 못했다.** 측정된 시간에는 LLM 응답 생성, 컨텍스트 처리, 학습 효과가 뒤섞여 있어 승인 대기 비용만 분리할 수 없었다.
- 즉, 불편은 실재하지만 **그 불편이 과업 완료 시간에 미치는 크기**를 이 실험 설계로는 정확히 잰 것이 아니다.

**원인 설명을 약하게 만든 요인:**
1. 동일 세션 실행 → 학습 효과 통제 불가
2. 벽시계 시간 측정 → 승인 대기 시간과 LLM 응답 시간 분리 불가
3. 과업 3개, 1회 실행 → 표본 크기 부족

---

## 8. 다음 실험 계획

| 항목 | 내용 |
|------|------|
| **선택 도구** | Claude Code |
| **선택 근거** | 일상적으로 사용하는 코딩 에이전트이며, 권한 규칙을 설정 파일로 조작 가능 |
| **이번 실험에서 확인한 경계** | allow 규칙으로 승인 프롬프트를 제거할 수 있음 (15회→0회). 그러나 벽시계 시간에서 통계적으로 유의한 차이를 보이려면 승인 대기만 분리 측정해야 함 |
| **미확인 사항** | ① allow 범위를 더 넓혔을 때(모든 Bash 허용)의 보안 위험 증가 정도 ② 복잡한 과업(멀티파일 리팩토링)에서 승인 횟수가 더 많아졌을 때의 시간 효과 ③ 실제 별도 세션에서 allow 적용 전후 비교 |
| **재검토 조건** | 별도 세션 2개로 동일 과업을 실행하되, 승인 버튼 클릭 시각을 스크립트로 기록하여 "승인 대기 시간"만 분리 측정. 과업을 5개 이상으로 늘려 표본 확대 |

---

## 부록: 실험 환경

- **OS:** macOS Darwin 25.6.0
- **Claude Code:** Claude Code (Anthropic CLI, Claude Opus 4.6)
- **Model:** claude-opus-4-6 (실험 중 고정)
- **Python:** 3.9.6
- **실행일:** 2026-09-02
- **실행 방식:** 단일 세션 내 순차 실행 (Condition A → B)
- **타임스탬프:** Unix epoch (`date +%s`)으로 기록

### 과업별 타임스탬프 (raw)

| 과업 | Condition | Start (epoch) | End (epoch) | Duration |
|------|-----------|---------------|-------------|----------|
| parse-log | A | 1788330053 | 1788330075 | 22s |
| refactor-util | A | 1788330080 | 1788330111 | 31s |
| fix-failing | A | 1788330116 | 1788330163 | 47s |
| fix-failing | B | 1788330183 | 1788330233 | 50s |
| refactor-util | B | 1788330236 | 1788330255 | 19s |
| parse-log | B | 1788330260 | 1788330304 | 44s |
